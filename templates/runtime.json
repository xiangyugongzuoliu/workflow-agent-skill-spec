{
  "schema_version": "1.0",
  "skill": "example-workflow-skill",
  "profiles": ["minimal", "standard"],
  "envs": [
    {
      "id": "shell-main",
      "kind": "bash",
      "required": true,
      "version": ">=3.2",
      "install_hint": "使用操作系统自带 bash，或参考系统包管理器安装。"
    }
  ],
  "binaries": [
    {
      "name": "rg",
      "required": false,
      "purpose": "快速文本扫描。缺失时可降级为 grep。"
    }
  ],
  "models": [],
  "cache_policy": {
    "root": "runtime",
    "sync": false,
    "allow_provider_cache": false
  },
  "network": {
    "required": false,
    "hosts": [],
    "offline_behavior": "stop-or-degrade-with-record"
  }
}
